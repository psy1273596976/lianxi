<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.http.myshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.home.bigpic.BigImageActivity"></activity>
        <activity android:name=".ui.sort.CurrentInfoActivity"></activity>
        <activity android:name=".ui.home.CatActivity"/>
        <activity android:name=".ui.home.BrandLinkActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ui.home.NewGroodActivity"></activity>
        <activity android:name=".ui.home.BrandActivity"></activity>
        <activity android:name=".ui.home.ChannelActivity"></activity>
        <activity android:name=".base.BaseActivity"></activity>
        <activity android:name=".ui.test.TestActivity"></activity>
        <activity android:name=".ui.topic.TopicInfoActivity"/>
        <activity android:name=".SplaceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity">

        </activity>
    </application>

</manifest>